import{_ as e,c as a,o as r,a as t}from"./app.80fd0906.js";const s="/assets/image-20220915111437134.28d91492.png",n="/assets/image-20220915111831773.9ce36c65.png",i="/assets/image-20220915115852613.456d0d76.png",o="/assets/image-20220915120431285.4edfb0a1.png",p="/assets/image-20220915120626131.942f2821.png",f=JSON.parse('{"title":"9.16-学习http(十二)","description":"","frontmatter":{},"headers":[{"level":2,"title":"HTTP的代理服务","slug":"http的代理服务","link":"#http的代理服务","children":[{"level":3,"title":"代理服务","slug":"代理服务","link":"#代理服务","children":[]},{"level":3,"title":"代理的作用","slug":"代理的作用","link":"#代理的作用","children":[]},{"level":3,"title":"代理相关头字段","slug":"代理相关头字段","link":"#代理相关头字段","children":[]},{"level":3,"title":"代理协议","slug":"代理协议","link":"#代理协议","children":[]},{"level":3,"title":"小结","slug":"小结","link":"#小结","children":[]}]},{"level":2,"title":"HTTP的缓存代理","slug":"http的缓存代理","link":"#http的缓存代理","children":[{"level":3,"title":"缓存代理服务","slug":"缓存代理服务","link":"#缓存代理服务","children":[]},{"level":3,"title":"源服务器的缓存控制","slug":"源服务器的缓存控制","link":"#源服务器的缓存控制","children":[]},{"level":3,"title":"客户端的缓存控制","slug":"客户端的缓存控制","link":"#客户端的缓存控制","children":[]},{"level":3,"title":"小结","slug":"小结-1","link":"#小结-1","children":[]}]},{"level":2,"title":"参考","slug":"参考","link":"#参考","children":[]}],"relativePath":"article/daily-study/2022/september/9.16-学习http(十二).md"}'),l={name:"article/daily-study/2022/september/9.16-学习http(十二).md"},d=t('<h1 id="_9-16-学习http-十二" tabindex="-1">9.16-学习http(十二) <a class="header-anchor" href="#_9-16-学习http-十二" aria-hidden="true">#</a></h1><h2 id="http的代理服务" tabindex="-1"><strong>HTTP的代理服务</strong> <a class="header-anchor" href="#http的代理服务" aria-hidden="true">#</a></h2><p>在前面讲 HTTP 协议的时候，我们严格遵循了 HTTP 的请求 - 应答模型，协议中只有两个互相通信的角色，分别是请求方浏览器(客户端)和应答方服务器。</p><p>今天，我们要在这个模型里引入一个新的角色，那就是HTTP 代理。</p><p>引入 HTTP 代理后，原来简单的双方通信就变复杂了一些，加入了一个或者多个中间人，但整体上来看，还是一个有顺序关系的链条，而且链条里相邻的两个角色仍然是简单的一对一通信，不会出现越级的情况。</p><img src="'+s+'" alt="image-20220915111437134" style="zoom:50%;"><p>链条的起点还是客户端(也就是浏览器)，中间的角色被称为代理服务器(proxy server)，链条的终点被称为源服务器(origin server)，意思是数据的源头起源。</p><h3 id="代理服务" tabindex="-1"><strong>代理服务</strong> <a class="header-anchor" href="#代理服务" aria-hidden="true">#</a></h3><p>代理服务就是指<code>服务本身不生产内容，而是处于中间位置转发上下游的请求和响应，具有双重身份</code>: 面向下游的用户时，表现为服务器，代表源服务器响应客户端的请求;而面向上游的源服务器时，又表现为客户端，代表客户端发送请求。</p><h3 id="代理的作用" tabindex="-1"><strong>代理的作用</strong> <a class="header-anchor" href="#代理的作用" aria-hidden="true">#</a></h3><p>为什么要有代理呢?换句话说，代理能干什么、带来什么好处呢?</p><p>你也许听过这样一句至理名言:计算机科学领域里的任何问题，都可以通过引入一个中间 层来解决(在这句话后面还可以再加上一句如果一个中间层解决不了问题，那就再加一 个中间层)。TCP/IP 协议栈是这样，而代理也是这样。</p><p>由于代理处在 HTTP 通信过程的中间位置，相应地就对上屏蔽了真实客户端，对下屏蔽了真实服务器，简单的说就是<strong>欺上瞒下</strong>。在这个中间层的小天地里就可以做很多的事情，为 HTTP 协议增加更多的灵活性，实现客户端和服务器的双赢。</p><p>代理最基本的一个功能是<strong>负载均衡</strong>。因为在面向客户端时屏蔽了源服务器，客户端看到的只是代理服务器，源服务器究竟有多少台、是哪些 IP 地址都不知道。于是代理服务器就可以 掌握请求分发的大权，决定由后面的哪台服务器来响应请求。</p><p>代理中常用的负载均衡算法你应该也有所耳闻吧，比如轮询、一致性哈希等等，这些算法的目标都是尽量把外部的流量合理地分散到多台源服务器，提高系统的整体资源利用率和性能。</p><p>在负载均衡的同时，代理服务还可以执行更多的功能，比如:</p><ul><li><strong>健康检查</strong>:使用心跳等机制监控后端服务器，发现有故障就及时踢出集群，保证服务高可用</li><li><strong>安全防护</strong>:保护被代理的后端服务器，限制 IP 地址或流量，抵御网络攻击和过载</li><li><strong>加密卸载</strong>:对外网使用 SSL/TLS 加密通信认证，而在安全的内网不加密，消除加解密成本</li><li><strong>数据过滤</strong>:拦截上下行的数据，任意指定策略修改请求或者响应</li><li><strong>内容缓存</strong>:暂存、复用服务器响应</li></ul><h3 id="代理相关头字段" tabindex="-1"><strong>代理相关头字段</strong> <a class="header-anchor" href="#代理相关头字段" aria-hidden="true">#</a></h3><p>代理的好处很多，但因为它欺上瞒下的特点，隐藏了真实客户端和服务器，如果双方想要获得这些丢失的原始信息，该怎么办呢?</p><p>首先，代理服务器需要用字段<strong>Via</strong>标明代理的身份。</p><p>Via 是一个通用字段，请求头或响应头里都可以出现。每当报文经过一个代理节点，代理服务器就会把自身的信息追加到字段的末尾，就像是经手人盖了一个章。</p><p>如果通信链路中有很多中间代理，就会在 Via 里形成一个链表，这样就可以知道报文究竟 走过了多少个环节才到达了目的地。</p><p>例如下图中有两个代理:proxy1 和 proxy2，客户端发送请求会经过这两个代理，依次添加就是Via: proxy1, proxy2，等到服务器返回响应报文的时候就要反过来走，头字段就是Via: proxy2, proxy1。</p><img src="'+n+`" alt="image-20220915111831773" style="zoom:50%;"><p>Via 字段只解决了客户端和源服务器判断是否存在代理的问题，还不能知道对方的真实信息</p><p>但服务器的 IP 地址应该是保密的，关系到企业的内网安全，所以一般不会让客户端知道。 不过反过来，通常服务器需要知道客户端的真实 IP 地址，方便做访问控制、用户画像、统计分析。</p><p>可惜的是 HTTP 标准里并没有为此定义头字段，但已经出现了很多事实上的标准，最常用的两个头字段是<strong>X-Forwarded-For</strong>和<strong>X-Real-IP</strong>。</p><p>X-Forwarded-For的字面意思是为谁而转发，形式上和Via差不多，也是每经过一个代理节点就会在字段里追加一个信息。但Via追加的是代理主机名(或者域名)，而X-Forwarded-For追加的是请求方的 IP 地址。所以，在字段里最左边的 IP 地址就客户端的地址。</p><p>X-Real-IP是另一种获取客户端真实 IP 的手段，它的作用很简单，就是记录客户端 IP 地址，没有中间的代理信息，相当于是X-Forwarded-For的简化版。如果客户端和源 服务器之间只有一个代理，那么这两个字段的值就是相同的。</p><h3 id="代理协议" tabindex="-1"><strong>代理协议</strong> <a class="header-anchor" href="#代理协议" aria-hidden="true">#</a></h3><p>有了X-Forwarded-For等头字段，源服务器就可以拿到准确的客户端信息了。但对于代理服务器来说它并不是一个最佳的解决方案。</p><p>因为通过X-Forwarded-For操作代理信息必须要解析 HTTP 报文头，这对于代理来说成本比较高，原本只需要简单地转发消息就好，而现在却必须要费力解析数据再修改数据， 会降低代理的转发性能。</p><p>另一个问题是X-Forwarded-For等头必须要修改原始报文，而有些情况下是不允许甚 至不可能的(比如使用 HTTPS 通信被加密)。</p><p>所以就出现了一个专门的代理协议(The PROXY protocol)，它由知名的代理软件 HAProxy 所定义，也是一个事实标准，被广泛采用(注意并不是 RFC)。</p><p>代理协议有 v1 和 v2 两个版本，v1 和 HTTP 差不多，也是明文，而 v2 是二进制格 式。今天只介绍比较好理解的 v1，它在 HTTP 报文前增加了一行 ASCII 码文本，相当于又 多了一个头。</p><p>这一行文本其实非常简单，开头必须是PROXY五个大写字母，然后是TCP4或 者TCP6，表示客户端的 IP 地址类型，再后面是请求方地址、应答方地址、请求方端口号、应答方端口号，最后用一个回车换行(\\r\\n)结束。</p><p>例如下面的这个例子，在 GET 请求行前多出了 PROXY 信息行，客户端的真实 IP 地址 是1.1.1.1，端口号是 55555。</p><div class="language-html"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki material-palenight"><code><span class="line"><span style="color:#A6ACCD;">PROXY TCP4 1.1.1.1 2.2.2.2 55555 80\\r\\n </span></span>
<span class="line"><span style="color:#A6ACCD;">GET / HTTP/1.1\\r\\n</span></span>
<span class="line"><span style="color:#A6ACCD;">Host: www.xxx.com\\r\\n</span></span>
<span class="line"><span style="color:#A6ACCD;">\\r\\n</span></span>
<span class="line"></span></code></pre></div><p>服务器看到这样的报文，只要解析第一行就可以拿到客户端地址，不需要再去理会后面的 HTTP 数据，省了很多事情。</p><p>不过代理协议并不支持X-Forwarded-For的链式地址形式，所以拿到客户端地址后再 如何处理就需要代理服务器与后端自行约定。</p><h3 id="小结" tabindex="-1"><strong>小结</strong> <a class="header-anchor" href="#小结" aria-hidden="true">#</a></h3><ul><li>HTTP 代理就是客户端和服务器通信链路中的一个中间环节，为两端提供代理服务</li><li>代理处于中间层，为 HTTP 处理增加了更多的灵活性，可以实现负载均衡、安全防护、 数据过滤等功能</li><li>代理服务器需要使用字段Via标记自己的身份，多个代理会形成一个列表</li><li>如果想要知道客户端的真实 IP 地址，可以使用字段X-Forwarded-For和X-Real-IP</li><li>专门的代理协议可以在不改动原始报文的情况下传递客户端的真实 IP</li></ul><h2 id="http的缓存代理" tabindex="-1"><strong>HTTP的缓存代理</strong> <a class="header-anchor" href="#http的缓存代理" aria-hidden="true">#</a></h2><p>之前谈到缓存时，主要讲了客户端(浏览器)上的缓存控制，它能够减少响应时间、节约带宽，提升客户端的用户体验。</p><p>但 HTTP 传输链路上，不只是客户端有缓存，服务器上的缓存也是非常有价值的，可以让请求不必走完整个后续处理流程，就近获得响应结果。</p><p>特别是对于那些读多写少的数据，例如突发热点新闻、爆款商品的详情页，一秒钟内可能有成千上万次的请求。即使仅仅缓存数秒钟，也能够把巨大的访问流量挡在外面，让RPS(request per second)降低好几个数量级，减轻应用服务器的并发压力，对性能的 改善是非常显著的。</p><p>HTTP 的服务器缓存功能主要由代理服务器来实现(即缓存代理)，而源服务器系统内部虽然也经常有各种缓存(如 Memcache、Redis、Varnish 等)，但与 HTTP 没有太多关系，所以这里暂且不说。</p><h3 id="缓存代理服务" tabindex="-1"><strong>缓存代理服务</strong> <a class="header-anchor" href="#缓存代理服务" aria-hidden="true">#</a></h3><p>代理服务收到源服务器发来的响应数据后需要做两件事。第一个当然是把报文转发给客户端，而第二个就是把报文存入自己的 Cache 里。</p><p>下一次再有相同的请求，代理服务器就可以直接发送 304 或者缓存数据，不必再从源服务器那里获取。这样就降低了客户端的等待时间，同时节约了源服务器的网络带宽。</p><img src="`+i+'" alt="image-20220915115852613" style="zoom:50%;"><p>在 HTTP 的缓存体系中，缓存代理的身份十分特殊，它既是客户端，又是服务器，同时也既不是客户端，又不是服务器。</p><p>说它即是客户端又是服务器，是因为它面向源服务器时是客户端，在面向客户端时又是服务器，所以它即可以用客户端的缓存控制策略也可以用服务器端的缓存控制策略，也就是说它可以同时使用各种Cache-Control属性。</p><p>但缓存代理也即不是客户端又不是服务器，因为它只是一个数据的中转站，并不是真正的数据消费者和生产者，所以还需要有一些新的Cache-Control属性来对它做特别的约束。</p><h3 id="源服务器的缓存控制" tabindex="-1"><strong>源服务器的缓存控制</strong> <a class="header-anchor" href="#源服务器的缓存控制" aria-hidden="true">#</a></h3><p>之前介绍了 4 种服务器端的Cache-Control属性:max-age、no_store、 no_cache 和 must-revalidate，应该还有印象吧?</p><p>这 4 种缓存属性可以约束客户端，也可以约束代理。</p><p>但客户端和代理是不一样的，客户端的缓存只是用户自己使用，而代理的缓存可能会为非常多的客户端提供服务。所以，需要对它的缓存再多一些限制条件。</p><p>首先，我们要区分客户端上的缓存和代理上的缓存，可以使用两个新属性<strong>private</strong>和<strong>public</strong>。</p><p>private表示缓存只能在客户端保存，是用户私有的，不能放在代理上与别人共 享。而public的意思就是缓存完全开放，谁都可以存，谁都可以用。</p><p>比如你登录论坛，返回的响应报文里用Set-Cookie添加了论坛 ID，这就属于私人数据，不能存在代理上。不然，别人访问代理获取了被缓存的响应就麻烦了。</p><p>其次，缓存失效后的重新验证也要区分开(即使用条件请求Last- modified和ETag)，<strong>must-revalidate</strong>是只要过期就必须回源服务器验证，而新的<strong>proxy-revalidate</strong>只要求代理的缓存过期后必须验证，客户端不必回源，只验证 到代理这个环节就行了。</p><p>再次，缓存的生存时间可以使用新的<strong>s-maxage</strong>(s 是 share 的意思，注意 maxage 中间没有-)，只限定在代理上能够存多久，而客户端仍然使用max_age。</p><p>还有一个代理专用的属性<strong>no-transform</strong>。代理有时候会对缓存下来的数据做一些优化，比如把图片生成 png、webp 等几种格式，方便今后的请求处理，而no- transform就会禁止这样做，不许偷偷摸摸搞小动作。</p><p>源服务器在设置完Cache-Control后必须要为报文加上Last- modified或ETag字段。否则，客户端和代理后面就无法使用条件请求来验证缓存是否有效，也就不会有 304 缓存重定向。</p><img src="'+o+'" alt="image-20220915120431285" style="zoom:50%;"><h3 id="客户端的缓存控制" tabindex="-1"><strong>客户端的缓存控制</strong> <a class="header-anchor" href="#客户端的缓存控制" aria-hidden="true">#</a></h3><p>客户端在 HTTP 缓存体系里要面对的是代理和源服务器，也必须区别对待，这里就直接上图了，来个看图说话。</p><img src="'+p+'" alt="image-20220915120626131" style="zoom:50%;"><p>max-age、no_store、no_cache 这三个属性在之前已经介绍过了，它们也是同样作用于代理和源服务器。</p><p>关于缓存的生存时间，多了两个新属性<strong>max-stale</strong>和<strong>min-fresh</strong>。 max-stale的意思是如果代理上的缓存过期了也可以接受，但不能过期太多，超过 x 秒</p><p>也会不要。min-fresh的意思是缓存必须有效，而且必须在 x 秒后依然有效。</p><p>比如，草莓上贴着标签max-age=5，现在已经在冰柜里存了 7 天。如果有请求max-stale=2，意思是过期两天也能接受，所以刚好能卖出去。</p><p>但要是min-fresh=1，这是绝对不允许过期的，就不会买走。这时如果有另外一个菠萝 是max-age=10，那么7+1&lt;10，在一天之后还是新鲜的，所以就能卖出去。</p><p>有的时候客户端还会发出一个特别的<strong>only-if-cached</strong>属性，表示只接受代理缓存的数 据，不接受源服务器的响应。如果代理上没有缓存或者缓存过期，就应该给客户端返回一个 504(Gateway Timeout)。</p><h3 id="小结-1" tabindex="-1"><strong>小结</strong> <a class="header-anchor" href="#小结-1" aria-hidden="true">#</a></h3><ul><li>计算机领域里最常用的性能优化手段是时空转换，也就是时间换空间或者空间换时间，HTTP 缓存属于后者</li><li>缓存代理是增加了缓存功能的代理服务，缓存源服务器的数据，分发给下游的客户端</li><li>Cache-Control字段也可以控制缓存代理，常用的有private、s-maxage、no-transform等，同样必须配合Last-modified、ETag等字段才能使用</li><li>缓存代理有时候也会带来负面影响，缓存不良数据，需要及时刷新或删除</li></ul><h2 id="参考" tabindex="-1">参考 <a class="header-anchor" href="#参考" aria-hidden="true">#</a></h2><p><a href="https://time.geekbang.org/column/intro/100029001" target="_blank" rel="noreferrer">透视HTTP协议(罗剑锋)</a></p>',79),h=[d];function g(c,T,m,P,_,u){return r(),a("div",null,h)}const C=e(l,[["render",g]]);export{f as __pageData,C as default};
